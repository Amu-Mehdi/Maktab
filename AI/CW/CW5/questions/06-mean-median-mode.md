# Problem 6: Analyzing Mean, Median, and Mode

For the `OrderValue` column, calculate:

* Mean
* Median
* Mode

Then calculate the mode of the following columns:

* `PaymentMethod`
* `City`

Based on the outputs, answer the following questions:

1. What value does the mean of `OrderValue` represent for the overall set of orders?
2. Into how many parts does the median divide the dataset?
3. Which payment method is represented by the mode of `PaymentMethod`?
4. Which city is represented by the mode of `City`?
5. Does `OrderValue` have a single unique mode? Examine the Pandas output.
6. Which measure is appropriate for the categorical variable `PaymentMethod`: mean, median, or mode?
7. Which measure is less affected by the value `900`: mean or median?
