# Problem 6: Training a Linear Regression Model

Create a Linear Regression model and train it using the Training data.

Then display the following values:

* `coef_`
* `intercept_`

Based on the output, answer the following questions:

1. Is the coefficient for `StudyHours` positive or negative?
2. What does the sign of the coefficient tell us about the relationship between study hours and final score?
3. What role does the `intercept_` play in the equation of the line?
4. In the equation `y = wx + b`, which model output represents `w`?
5. Which model output represents `b`?