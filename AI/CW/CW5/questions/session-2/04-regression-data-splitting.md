# Problem 4: Splitting Data for Regression

For predicting `FinalScore`:

1. Select `StudyHours` as the Feature.
2. Select `FinalScore` as the Target.
3. Split the data into Training and Test sets using `test_size=0.2` and `random_state=42`.